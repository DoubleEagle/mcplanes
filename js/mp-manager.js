function MpManager(){
	/*this.socket = io.connect('localhost:8080');
	
	socket.on('asdf', function (data) {
		console.log(data);
		socket.emit('my other event', {my:'data'});
	});
	socket.emit('my other event', {my:'data'});*/
}